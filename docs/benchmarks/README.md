# Benchmark board output

Run `viki evals run`, `viki evals compare`, and `viki evals publish` to generate benchmark proof artifacts here.

Included baseline reports:
- `benchmarks/baselines/codex.json`
- `benchmarks/baselines/claude_code.json`
- `benchmarks/baselines/opencode.json`
